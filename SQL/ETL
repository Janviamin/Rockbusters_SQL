SELECT *
FROM film
ORDER BY title ASC,
release_year DESC,
rental_rate DESC

SELECT rating,
AVG (rental_rate)
FROM film
GROUP BY rating

SELECT rating,
MIN (rental_duration),
MAX (rental_duration)
FROM film
GROUP BY rating

